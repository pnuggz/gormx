version: '3.6'

services:
    mysql:
        image: mysql:8.0 # intel only
        ports:
            - 3366:3306
        environment:
            - MYSQL_ROOT_PASSWORD=gormx
            - MYSQL_USER=gormx
            - MYSQL_PASSWORD=gormx
            - MYSQL_DATABASE=gormx
